const MonsterDex = () => { // contains similar folders cards system but instead has cards that you can fill in info into slots as you learn(can be attached to notes)
    return ('Munster')
}

export default MonsterDex